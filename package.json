{
  "name": "ai-risk-report-generator",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/chrisganaeshfx/ai-risk-report-generator.git",
  "author": "",
  "license": "MIT",
  "scripts": {
    "install:all": "yarn install && yarn --cwd client install && yarn --cwd server install",
    "dev": "concurrently \"yarn --cwd client dev\" \"yarn --cwd server dev\"",
    "test": "concurrently \"yarn --cwd client test --run\" \"yarn --cwd server test --run\""
  },
  "dependencies": {
    "concurrently": "^10.0.5"
  }
}
